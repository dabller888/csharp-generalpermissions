	//控制器
    var UserRole = new UserRole {    
        UserId = query.UserId,
        RoleId = query.RoleId,
    };

	//ui	
	$("#eUserId").val(data.Content.UserId);
$("#eRoleId").val(data.Content.RoleId);

